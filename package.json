{
  "name": "metadata-compare-tool",
  "private": true,
  "description": "Development tools and browser tests for Salesforce Metadata Compare.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "lint": "eslint scripts/ui/",
    "lint:fix": "eslint scripts/ui/ --fix",
    "format": "prettier --write scripts/ui/"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
